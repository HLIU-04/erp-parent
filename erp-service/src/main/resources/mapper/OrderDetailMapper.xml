<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.erp.mapper.OrderDetailMapper">

    <insert id="insertBatch" parameterType="list">
        insert into sale_order_detail(order_id, product_id, quantity, unit_price, total_price, create_time, update_time)
        values
            <foreach collection="list" item="item" separator=",">
                (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.unitPrice}, #{item.totalPrice},#{item.createTime}, #{item.updateTime})
            </foreach>
    </insert>
</mapper>